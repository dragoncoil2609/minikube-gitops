apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: reactsurvey
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
