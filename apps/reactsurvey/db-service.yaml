apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: reactsurvey
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  selector:
    app: db
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
