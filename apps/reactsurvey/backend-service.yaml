apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: reactsurvey
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
